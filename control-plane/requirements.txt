# Fusion CDC Engine - Control Plane Dependencies

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1
pymysql==1.1.0
pymongo==4.6.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-keycloak==3.9.1
cryptography==42.0.0

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Redis
redis==5.0.1

# Kafka — used by control-plane /api/v1/monitoring/health to probe the broker
# (KAFKA_BOOTSTRAP_SERVERS). The health check falls back to a raw TCP socket
# probe when the client library is unavailable, so this is a soft dependency.
kafka-python==2.0.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Utilities
python-dotenv==1.0.1
requests==2.31.0

# GraphQL — spec §1 (P1-5)
strawberry-graphql[fastapi]==0.219.2

# SSH Tunnel support
sshtunnel==0.4.0
paramiko==3.4.0

# Iceberg destination Test Connection / write-permission validation
# (mirrors transform-worker/iceberg_writer.py catalog factory). Used lazily
# by app/utils/iceberg_tester.py so the control-plane still boots if these
# are absent; the Iceberg test endpoints surface a clear error instead.
pyiceberg[glue,s3]==0.7.1
pyarrow==16.0.0
boto3==1.34.101

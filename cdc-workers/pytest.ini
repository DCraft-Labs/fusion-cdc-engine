[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests that require running Docker services (deselect with -m "not integration")
    load: marks load/benchmark tests (deselect with -m "not load")
